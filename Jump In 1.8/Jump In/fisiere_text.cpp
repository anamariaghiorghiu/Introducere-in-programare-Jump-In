/*
#include "fstream"


    srand(time(NULL));
    int x = rand() % 60 +1;
    std::string s = "nivel";
    char buffer[100] = {};
    std::string k = itoa(x, buffer, 10);
    std::string rez = s + "_" + k + ".in";
    std::cout << rez;
*/


